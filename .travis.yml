language: node_js
node_js:
- "10.9"
os: osx
sudo: false

cache:
  directories:
  - node_modules

compiler:
  - clang
  - gcc

script:
- cd app/view
- npm i
- npm run build --prod --aot
- cd ../background
- npm i
- npm run build --prod --aot
